<h1 class="text-2xl font-bold mb-4">Mon profil</h1>

<p><strong>Nom :</strong> <%= @user.first_name %> <%= @user.last_name %></p>

<p><strong>Email :</strong> <%= @user.email_address %></p>
<p>
  <strong>Image :</strong><br>
  <% if @user.image_url.present? %>
    <img src="<%= @user.image_url %>" alt="Avatar" class="w-20 h-20 rounded-full">
  <% else %>
    Pas d'image
  <% end %>
</p>

<%= link_to "Éditer mon profil", edit_profile_path, class: "mt-4 inline-block px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700" %>
