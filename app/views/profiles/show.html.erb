<div class="p-6 mt-6 mx-auto md:w-2/3 w-full rounded-lg bg-white shadow-md">
  <h1 class="font-bold text-4xl mb-5">Mon profil</h1>

  <% if @user %>
    <p><strong>Email :</strong> <%= @user.email_address %></p>
    <p><strong>Date d'inscription :</strong> <%= @user.created_at.strftime("%d %B %Y") %></p>
  <% else %>
    <p class="text-red-500">Aucun utilisateur connecté.</p>
  <% end %>
</div>
